# Default properties file automatically detected by spring. 
# Override this by using --spring.config.location=file:///otherdir/another-location.properties 
# at startup command of this app. For overriding specific properties directly without a file, you can 
# use --property="value" in command, e.g. --scan.cron="* * * * * *".
# For more information about the properties listed below, read: https://github.com/thehyve/cbioportal-staging/blob/master/README.md

# A cron-like expression (UN*X definition)
scan.cron=0 0 0 * * *
# number of times to repeat the cron (set to -1 to repeat indefinitely):
scan.cron.iterations=-1
# location to scan (local file system or S3 location):
scan.location=s3://bucket/staging_dir

# working directory
etl.working.dir=file:///tmp

# environment settings needed by S3 library
cloud.aws.region.static=eu-central-1
# (optional) aws credentials for access to S3 bucket:
#cloud.aws.credentials.accessKey=<aws-key>
#cloud.aws.credentials.secretKey=<aws-secret-key>

# cbioportal mode, either "local" or "docker".
cbioportal.mode=local
# properties for cbioportal.mode=docker:
cbioportal.docker.image=<image-name>
cbioportal.docker.network=<network-name>

# Central share location (for validation reports and log files).
central.share.location=file:///Users/oleguer/Desktop/test_out/share
# (optional) Central share location web-portal
#central.share.location.portal=https://s3.console.aws.amazon.com/s3/buckets/my_bucket/myreports_folder

# Minutes before trying to find a file specified by the yaml file that has not been found
time.attempt=5

# Full transformation command and path to execute it
transformation.command.script=./umccr_wrapper_parser.py
transformation.command.location=<location-path>

#Refresh command
refresh.command=catalina

#Validation level
validation.level = ERROR

#Mail properties
mail.admin.user=<email>
mail.app.user=noreply@thehyve.nl
mail.app.password=thehyvetest
mail.smtp.host=<smtp-host>
mail.debug=<boolean>
mail.transport.protocol=<transport-protocol>
mail.smtp.port=<smtp-port>
mail.smtp.auth=<boolean>
mail.smtp.ssl.enable=<boolean>
mail.smtp.starttls.enable=<boolean>

